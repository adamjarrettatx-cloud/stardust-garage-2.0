export const revalidate = 0;

export default function MemberDashboard() {
  return (
    <main style={{ padding: 40, color: 'white' }}>
      <h1>Member Area Test</h1>
      <p>If you can see this, the /member route is working.</p>
    </main>
  );
}
